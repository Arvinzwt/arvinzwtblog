import {Metadata} from 'next';

export const metadata: Metadata = {
  title: 'Create Blog',
};

export default async function Page() {
  return (
    <main>
      this is create page
    </main>
  );
}
